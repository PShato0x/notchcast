# Generate the Xcode project with XcodeGen:
#   brew install xcodegen
#   cd ios && xcodegen generate
#
# Before building, change:
#   1. bundleIdPrefix below (and the widget's PRODUCT_BUNDLE_IDENTIFIER)
#   2. The App Group in App.entitlements, Widget.entitlements, and Shared/Models.swift
#   3. DEVELOPMENT_TEAM to your Apple team ID (or set signing in Xcode)

name: ClaudeWidget
options:
  bundleIdPrefix: com.yourname
  deploymentTarget:
    iOS: "17.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    # DEVELOPMENT_TEAM: YOURTEAMID

targets:
  ClaudeWidget:
    type: application
    platform: iOS
    sources:
      - App
      - Shared
    info:
      path: App/Info.plist
      properties:
        CFBundleDisplayName: Claude Widget
        UILaunchScreen: {}
        CFBundleURLTypes:
          - CFBundleURLSchemes: [claudewidget]
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
        NSLocalNetworkUsageDescription: Connects to the Claude Widget relay running on your Mac.
    entitlements:
      path: App/App.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.yourname.claudewidget
    dependencies:
      - target: ClaudeStatusWidgetExtension
        embed: true

  ClaudeStatusWidgetExtension:
    type: app-extension
    platform: iOS
    sources:
      - Widget
      - Shared
    info:
      path: Widget/Info.plist
      properties:
        CFBundleDisplayName: Claude Code
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
        NSAppTransportSecurity:
          NSAllowsLocalNetworking: true
    entitlements:
      path: Widget/Widget.entitlements
      properties:
        com.apple.security.application-groups:
          - group.com.yourname.claudewidget
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.yourname.ClaudeWidget.widget
