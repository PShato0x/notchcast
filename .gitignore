# macOS
.DS_Store

# Node
node_modules/

# macOS island build artifacts
macos/NotchAI.app/
macos/.build/

# Xcode (generated by XcodeGen — regenerate with `xcodegen generate`)
ios/*.xcodeproj
ios/*.xcworkspace
xcuserdata/
DerivedData/
build/

# Local secrets — never commit tokens
config.local.json
